spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
#      name: shopping
      name: user-service #native-file-repo 디렉토리 하위의 user-service.yml 파일
#      name: config-service
#  profiles:
#    active: dev
